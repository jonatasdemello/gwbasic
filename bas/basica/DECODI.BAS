 ' DECODIFICADOR

 CLS
 ON ERROR GOTO 400
 LET A2$ = "ZYXWVUTSRQPONMLKJIHGFEDCBA"
 LET A = ASC("A")
 LET Z = ASC("Z")
 PRINT " Qual e' a chave:";
 INPUT CH: IF CH <> 2 THEN PRINT " CHAVE ERRADA !!!": END
 PRINT : PRINT " Digite FIM para teminar. "
180  PRINT : PRINT " Mensagem:";
 INPUT M$
 IF M$ = "FIM" THEN END
 FOR I = 1 TO LEN(M$)
 LET P$ = MID$(M$, I, 1)
 LET C = ASC(P$)
 IF C < A OR C > Z THEN GOTO 280
 LET P = C + CH
 IF P > Z THEN P = P - 26
 LET P = P - A + 1
 LET P$ = MID$(A2$, P, 1)
280 PRINT P$;
 NEXT I
 PRINT
 PRINT
 GOTO 180
400 RESUME 0


